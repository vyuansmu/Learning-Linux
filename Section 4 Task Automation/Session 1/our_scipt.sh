#!/bin/bash

# that line above is the location of where bash is, can edit it to location of where python3.8 is to make a python script aswell as long as the hash and shabang appears
# to make a comment in a bash script use this symbol
# use bash (script name).sh in the command line to execute the script

mkdir ~Desktop/magic/
cd ~/Desktop/magic
touch file{1-100}.txt
ls -lh ~/Desktop/magic > ~Desktop/magic.log

